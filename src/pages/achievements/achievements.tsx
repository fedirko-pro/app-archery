import './achievements.scss';

import tmp from './tmp.webp';

const Achievements = () => {
  return (
    <div className="profile-container">
      <img src={tmp} width="100%" alt="tmp" />
    </div>
  );
};

export default Achievements;
